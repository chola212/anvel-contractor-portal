import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="Portal configuration will stay limited to approved operational settings and security controls."
      nextPhase="later approved phases"
    />
  );
}
